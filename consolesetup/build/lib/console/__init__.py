"""package containing APIs to interact with the library and the text representation to produce a result of the work
of the library

_________

Description of internal packages

parser_api - a package that contains a set of tools that allow any user to work with the library "takinata_lib".
Provides a set of interfaces to interact with the library.

presentations - the package that defines the views for all entities
__________

Description of internal files

config.py - the console package configuration file. The library configuration first tries to get the configuration of
the console package, and if it is not available, it will use its own configuration

start.py - the main package file that receives arguments and sends them for processing

"""